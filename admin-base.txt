==================================================
FILE: app/layout.tsx
==================================================
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "STK Bakery",
  description:
    "Professional bakery website, cake builder and order management platform.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ru">
      <body>{children}</body>
    </html>
  );
}

==================================================
FILE: app/globals.css
==================================================
@import "tailwindcss";

:root {
  --background: #fffaf7;
  --foreground: #312422;
  --white: #ffffff;
  --pink: #ec4d84;
  --pink-dark: #d83a72;
  --pink-light: #ffe8f0;
  --cream: #f7ede7;
  --brown: #382825;
  --muted: #786966;
  --border: rgba(56, 40, 37, 0.12);
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}

a {
  color: inherit;
  text-decoration: none;
}

button,
input,
textarea,
select {
  font: inherit;
}

img {
  max-width: 100%;
}

.container {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}

/* Header */

.site-header {
  position: sticky;
  top: 0;
  z-index: 50;
  border-bottom: 1px solid rgba(56, 40, 37, 0.07);
  background: rgba(255, 250, 247, 0.91);
  backdrop-filter: blur(18px);
}

.header-inner {
  min-height: 82px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 28px;
}

.brand {
  display: inline-flex;
  align-items: center;
  gap: 11px;
}

.brand-logo {
  width: 43px;
  height: 43px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--pink);
  color: var(--white);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 14px;
  font-weight: 700;
}

.brand-text {
  font-family: Georgia, "Times New Roman", serif;
  font-size: 28px;
  font-style: italic;
  letter-spacing: -1px;
}

.brand-text strong {
  color: var(--pink);
}

.desktop-nav {
  display: flex;
  align-items: center;
  gap: 30px;
  color: #594945;
  font-size: 14px;
  font-weight: 600;
}

.desktop-nav a {
  transition:
    color 0.2s ease,
    transform 0.2s ease;
}

.desktop-nav a:hover {
  color: var(--pink);
  transform: translateY(-1px);
}

.header-button {
  padding: 13px 21px;
  border-radius: 999px;
  background: var(--brown);
  color: var(--white);
  font-size: 14px;
  font-weight: 700;
  transition:
    background 0.2s ease,
    transform 0.2s ease;
}

.header-button:hover {
  background: var(--pink);
  transform: translateY(-2px);
}

/* Hero */

.hero {
  position: relative;
  min-height: 720px;
  display: flex;
  align-items: center;
  overflow: hidden;
  background:
    radial-gradient(circle at 78% 34%, #ffe7ef 0, transparent 32%),
    linear-gradient(135deg, #fffaf7 0%, #fff4f6 100%);
}

.hero-circle {
  position: absolute;
  border-radius: 50%;
}

.hero-circle-one {
  top: 70px;
  right: -100px;
  width: 320px;
  height: 320px;
  background: rgba(236, 77, 132, 0.07);
}

.hero-circle-two {
  bottom: 0;
  left: -100px;
  width: 220px;
  height: 220px;
  background: rgba(228, 205, 192, 0.45);
}

.hero-grid {
  position: relative;
  z-index: 2;
  display: grid;
  grid-template-columns: 0.95fr 1.05fr;
  align-items: center;
  gap: 65px;
  padding-top: 72px;
  padding-bottom: 86px;
}

.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 23px;
  color: var(--pink-dark);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.09em;
  text-transform: uppercase;
}

.eyebrow span {
  width: 35px;
  height: 1px;
  background: var(--pink);
}

.hero h1 {
  margin: 0;
  color: var(--brown);
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(52px, 6vw, 80px);
  font-weight: 500;
  line-height: 0.99;
  letter-spacing: -4px;
}

.hero h1 em {
  color: var(--pink);
  font-weight: 500;
}

.hero-description {
  max-width: 620px;
  margin: 28px 0 0;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.75;
}

.hero-actions {
  display: flex;
  align-items: center;
  gap: 15px;
  margin-top: 36px;
}

.primary-button,
.secondary-button,
.dark-button {
  min-height: 56px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  border-radius: 999px;
  font-weight: 800;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease,
    background 0.2s ease;
}

.primary-button {
  padding: 0 25px;
  background: var(--pink);
  color: var(--white);
  box-shadow: 0 15px 35px rgba(236, 77, 132, 0.23);
}

.primary-button:hover {
  background: var(--pink-dark);
  transform: translateY(-3px);
  box-shadow: 0 18px 38px rgba(236, 77, 132, 0.3);
}

.secondary-button {
  padding: 0 24px;
  border: 1px solid var(--border);
  background: rgba(255, 255, 255, 0.68);
  color: var(--brown);
}

.secondary-button:hover {
  background: var(--white);
  transform: translateY(-3px);
}

.hero-information {
  display: flex;
  gap: 38px;
  margin-top: 43px;
  padding-top: 25px;
  border-top: 1px solid var(--border);
}

.hero-information div {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.hero-information strong {
  font-size: 14px;
}

.hero-information span {
  color: var(--muted);
  font-size: 12px;
}

.hero-visual {
  position: relative;
  min-height: 555px;
  display: grid;
  place-items: center;
}

.hero-photo {
  position: relative;
  width: min(500px, 100%);
  height: 555px;
  overflow: hidden;
  border-radius: 245px 245px 30px 30px;
  background: linear-gradient(145deg, #f8dce5, #eebecf);
  box-shadow: 0 38px 85px rgba(76, 48, 42, 0.2);
}

.hero-photo {
  position: relative;
  width: min(500px, 100%);
  height: 555px;
  overflow: hidden;
  border-radius: 245px 245px 30px 30px;
  background: linear-gradient(145deg, #f8dce5, #eebecf);
  box-shadow: 0 38px 85px rgba(76, 48, 42, 0.2);
}

.hero-photo-overlay {
  position: absolute;
  inset: 0;
  z-index: 1;
  background: linear-gradient(
    to top,
    rgba(44, 27, 24, 0.42),
    transparent 45%
  );
  pointer-events: none;
}

.hero-photo-caption {
  position: absolute;
  z-index: 2;
  right: 22px;
  bottom: 22px;
  left: 22px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 17px 19px;
  border: 1px solid rgba(255, 255, 255, 0.42);
  border-radius: 18px;
  background: rgba(255, 255, 255, 0.86);
  backdrop-filter: blur(14px);
}
.hero-photo-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    rgba(44, 27, 24, 0.42),
    transparent 45%
  );
  pointer-events: none;
}

.hero-photo-caption {
  position: absolute;
  z-index: 2;
  right: 22px;
  bottom: 22px;
  left: 22px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 17px 19px;
  border: 1px solid rgba(255, 255, 255, 0.42);
  border-radius: 18px;
  background: rgba(255, 255, 255, 0.86);
  backdrop-filter: blur(14px);
}

.hero-photo-caption div {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.hero-photo-caption span {
  color: var(--pink-dark);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.hero-photo-caption strong {
  font-family: Georgia, "Times New Roman", serif;
  font-size: 18px;
  font-weight: 500;
}

.hero-photo-caption a {
  white-space: nowrap;
  font-size: 13px;
  font-weight: 800;
}

.hero-badge {
  position: absolute;
  z-index: 5;
  top: 16px;
  right: -5px;
  width: 122px;
  height: 122px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(236, 77, 132, 0.32);
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.84);
  color: var(--pink-dark);
  text-align: center;
  transform: rotate(7deg);
  backdrop-filter: blur(12px);
}

.hero-badge span {
  font-family: Georgia, "Times New Roman", serif;
  font-size: 14px;
  font-style: italic;
}

.hero-badge strong {
  margin-top: 3px;
  font-size: 12px;
}

.floating-card {
  position: absolute;
  z-index: 6;
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 13px 16px;
  border: 1px solid rgba(255, 255, 255, 0.85);
  border-radius: 17px;
  background: rgba(255, 255, 255, 0.88);
  box-shadow: 0 16px 35px rgba(79, 49, 43, 0.14);
  backdrop-filter: blur(12px);
}

.floating-icon {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--pink-light);
  font-size: 20px;
}

.floating-card div {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.floating-card strong {
  font-size: 13px;
}

.floating-card small {
  color: var(--muted);
  font-size: 10px;
}

.floating-card-left {
  top: 145px;
  left: -36px;
}

.floating-card-right {
  right: -35px;
  bottom: 82px;
}

/* Shared */

.categories-section,
.popular-section,
.advantages-section,
.process-section {
  padding: 110px 0;
}

.categories-section,
.popular-section {
  background: var(--white);
}

.section-header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 35px;
  margin-bottom: 48px;
}

.section-header h2,
.builder-content h2,
.process-heading h2,
.final-section h2 {
  margin: 9px 0 0;
  color: var(--brown);
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(38px, 4.5vw, 58px);
  font-weight: 500;
  line-height: 1.05;
  letter-spacing: -2px;
}

.section-header > p {
  max-width: 440px;
  margin: 0;
  color: var(--muted);
  line-height: 1.7;
}

.section-label {
  color: var(--pink-dark);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.13em;
  text-transform: uppercase;
}

/* Categories */

.categories-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}

.category-card {
  position: relative;
  min-height: 355px;
  display: flex;
  flex-direction: column;
  padding: 34px;
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 28px;
  background: #fffaf8;
  transition:
    transform 0.25s ease,
    box-shadow 0.25s ease;
}

.category-card:hover {
  transform: translateY(-7px);
  box-shadow: 0 25px 55px rgba(77, 49, 43, 0.1);
}

.category-card-featured {
  border-color: transparent;
  background:
    radial-gradient(
      circle at 100% 0,
      rgba(255, 255, 255, 0.24),
      transparent 36%
    ),
    var(--pink);
  color: var(--white);
  box-shadow: 0 24px 55px rgba(236, 77, 132, 0.23);
}

.category-popular {
  position: absolute;
  top: 23px;
  right: 23px;
  padding: 8px 12px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.18);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.07em;
  text-transform: uppercase;
}

.category-icon {
  width: 63px;
  height: 63px;
  display: grid;
  place-items: center;
  margin-bottom: 42px;
  border-radius: 19px;
  background: var(--white);
  box-shadow: 0 12px 25px rgba(70, 47, 42, 0.09);
  font-size: 29px;
}

.category-card h3 {
  margin: 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 27px;
  font-weight: 500;
}

.category-card p {
  margin: 15px 0 25px;
  color: var(--muted);
  font-size: 14px;
  line-height: 1.7;
}

.category-card-featured p {
  color: rgba(255, 255, 255, 0.79);
}

.category-card > a {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  margin-top: auto;
  font-size: 14px;
  font-weight: 800;
}

/* Builder section */

.builder-section {
  padding: 115px 0;
  overflow: hidden;
  background: var(--cream);
}

.builder-grid {
  display: grid;
  grid-template-columns: 1.08fr 0.92fr;
  align-items: center;
  gap: 85px;
}

.builder-preview {
  position: relative;
}

.builder-preview::before {
  content: "";
  position: absolute;
  inset: 50px -40px -35px 35px;
  border-radius: 40px;
  background: rgba(236, 77, 132, 0.12);
  transform: rotate(-4deg);
}

.builder-window {
  position: relative;
  z-index: 2;
  overflow: hidden;
  border: 8px solid var(--white);
  border-radius: 28px;
  background: var(--white);
  box-shadow: 0 38px 80px rgba(73, 48, 42, 0.17);
  transform: rotate(1.5deg);
}

.builder-window-header {
  height: 52px;
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 0 18px;
  border-bottom: 1px solid var(--border);
}

.window-controls {
  display: flex;
  gap: 7px;
}

.window-controls span {
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: #e7d7d0;
}

.window-controls span:first-child {
  background: #f47791;
}

.builder-window-header small {
  color: var(--muted);
}

.builder-window-body {
  min-height: 390px;
  display: grid;
  grid-template-columns: 145px 1fr;
  background: #fffaf8;
}

.builder-menu {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 24px 17px;
  border-right: 1px solid var(--border);
  background: var(--white);
}

.builder-menu button {
  padding: 12px 13px;
  border: 0;
  border-radius: 12px;
  background: transparent;
  color: var(--muted);
  text-align: left;
  cursor: pointer;
  font-size: 12px;
  font-weight: 700;
}

.builder-menu button.active {
  background: var(--pink-light);
  color: var(--pink-dark);
}

.builder-photo {
  position: relative;
  min-width: 0;
  overflow: hidden;
  background: linear-gradient(145deg, #fce8ee, #f5d5df);
}




.builder-photo span {
  position: absolute;
  right: 15px;
  bottom: 15px;
  padding: 9px 12px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.88);
  color: var(--pink-dark);
  font-size: 10px;
  font-weight: 800;
}

.builder-content h2 em {
  color: var(--pink);
  font-weight: 500;
}

.builder-content > p {
  margin: 25px 0;
  color: var(--muted);
  font-size: 16px;
  line-height: 1.75;
}

.builder-content ul {
  display: grid;
  gap: 13px;
  margin: 0 0 31px;
  padding: 0;
  list-style: none;
}

.builder-content li {
  display: flex;
  align-items: center;
  gap: 12px;
  color: #544641;
  font-size: 14px;
}

.builder-content li span {
  width: 24px;
  height: 24px;
  display: grid;
  flex-shrink: 0;
  place-items: center;
  border-radius: 50%;
  background: var(--white);
  color: var(--pink);
  font-size: 12px;
  font-weight: 900;
}

/* Products */

.catalog-link {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  font-size: 14px;
  font-weight: 800;
}

.products-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 25px;
}

.product-card {
  min-width: 0;
}

.product-image {
  position: relative;
  height: 365px;
  display: grid;
  place-items: center;
  overflow: hidden;
  border-radius: 25px;
  background: linear-gradient(145deg, #f9e1e8, #efd0da);
}

.product-image img {
  position: relative;
  z-index: 2;
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
}

.product-placeholder {
  position: absolute;
  z-index: 1;
  color: rgba(56, 40, 37, 0.48);
  font-size: 13px;
}

.product-image button {
  position: absolute;
  z-index: 4;
  top: 18px;
  right: 18px;
  width: 42px;
  height: 42px;
  border: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.9);
  color: var(--brown);
  cursor: pointer;
  font-size: 22px;
}

.product-details {
  display: flex;
  justify-content: space-between;
  gap: 15px;
  padding: 21px 5px 0;
}

.product-details span {
  color: var(--pink-dark);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.09em;
  text-transform: uppercase;
}

.product-details h3 {
  margin: 7px 0 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 22px;
  font-weight: 500;
}

.product-details strong {
  padding-top: 20px;
  white-space: nowrap;
  font-size: 15px;
}

/* Advantages */

.advantages-section {
  padding-top: 0;
  background: var(--white);
}

.advantages-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  border-top: 1px solid var(--border);
}

.advantage-card {
  padding: 48px 42px 20px;
  border-right: 1px solid var(--border);
}

.advantage-card:last-child {
  border-right: 0;
}

.advantage-card > span {
  color: var(--pink);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 23px;
  font-style: italic;
}

.advantage-card h3 {
  margin: 18px 0 10px;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 24px;
  font-weight: 500;
}

.advantage-card p {
  max-width: 285px;
  margin: 0;
  color: var(--muted);
  font-size: 14px;
  line-height: 1.7;
}

/* Process */

.process-section {
  background: #fff7f4;
}

.process-heading {
  max-width: 680px;
  margin-bottom: 52px;
}

.process-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18px;
}

.process-grid article {
  min-height: 240px;
  padding: 28px;
  border: 1px solid var(--border);
  border-radius: 23px;
  background: var(--white);
}

.process-grid article > span {
  width: 41px;
  height: 41px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--pink-light);
  color: var(--pink-dark);
  font-weight: 900;
}

.process-grid h3 {
  margin: 33px 0 11px;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 21px;
  font-weight: 500;
}

.process-grid p {
  margin: 0;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.7;
}

/* CTA */

.final-section {
  padding: 90px 0;
  background: var(--pink);
  color: var(--white);
}

.final-section-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 50px;
}

.final-section .section-label {
  color: rgba(255, 255, 255, 0.73);
}

.final-section h2 {
  max-width: 760px;
  color: var(--white);
}

.final-section p {
  max-width: 690px;
  margin: 20px 0 0;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.7;
}

.dark-button {
  flex-shrink: 0;
  padding: 0 27px;
  background: var(--brown);
  color: var(--white);
}

.dark-button:hover {
  background: #201513;
  transform: translateY(-3px);
}

/* Footer */

.site-footer {
  padding: 75px 0 25px;
  background: #2f2321;
  color: var(--white);
}

.footer-grid {
  display: grid;
  grid-template-columns: 1.5fr 0.7fr 0.7fr 1fr;
  gap: 55px;
}

.footer-brand .brand-logo {
  background: rgba(255, 255, 255, 0.12);
}

.footer-about p {
  max-width: 290px;
  margin: 20px 0 0;
  color: rgba(255, 255, 255, 0.58);
  font-size: 14px;
  line-height: 1.7;
}

.footer-column {
  display: flex;
  flex-direction: column;
  gap: 13px;
}

.footer-column strong {
  margin-bottom: 7px;
  font-size: 13px;
}

.footer-column a,
.footer-column span {
  color: rgba(255, 255, 255, 0.58);
  font-size: 13px;
}

.footer-phone {
  color: var(--white) !important;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 20px !important;
}

.footer-bottom {
  display: flex;
  justify-content: space-between;
  gap: 20px;
  margin-top: 65px;
  padding-top: 24px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  color: rgba(255, 255, 255, 0.42);
  font-size: 11px;
}

/* Tablet */

@media (max-width: 1050px) {
  .desktop-nav {
    display: none;
  }

  .hero-grid {
    grid-template-columns: 1fr;
  }

  .hero-content {
    text-align: center;
  }

  .eyebrow,
  .hero-actions,
  .hero-information {
    justify-content: center;
  }

  .hero-description {
    margin-right: auto;
    margin-left: auto;
  }

  .hero-visual {
    width: min(620px, 100%);
    margin: 0 auto;
  }

  .builder-grid {
    grid-template-columns: 1fr;
  }

  .builder-preview {
    width: min(680px, 100%);
    margin: 0 auto;
  }

  .process-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .footer-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 800px) {
  .categories-grid,
  .products-grid {
    grid-template-columns: 1fr;
  }

  .section-header {
    align-items: flex-start;
    flex-direction: column;
  }

  .advantages-grid {
    grid-template-columns: 1fr;
  }

  .advantage-card {
    padding: 32px 10px;
    border-right: 0;
    border-bottom: 1px solid var(--border);
  }

  .final-section-inner {
    align-items: flex-start;
    flex-direction: column;
  }
}

/* Mobile */

@media (max-width: 600px) {
  .container {
    width: min(100% - 26px, 1180px);
  }

  .header-inner {
    min-height: 70px;
  }

  .brand-text {
    font-size: 21px;
  }

  .brand-logo {
    width: 36px;
    height: 36px;
    font-size: 11px;
  }

  .header-button {
    padding: 11px 14px;
    font-size: 11px;
  }

  .hero {
    min-height: auto;
  }

  .hero-grid {
    gap: 38px;
    padding-top: 52px;
    padding-bottom: 62px;
  }

  .hero h1 {
    font-size: 47px;
    letter-spacing: -2.5px;
  }

  .hero-description {
    font-size: 15px;
  }

  .hero-actions {
    align-items: stretch;
    flex-direction: column;
  }

  .hero-actions a {
    width: 100%;
  }

  .hero-information {
    gap: 15px;
    justify-content: space-between;
    text-align: left;
  }

  .hero-visual {
    min-height: 460px;
  }

  .hero-photo {
    height: 445px;
    border-radius: 190px 190px 25px 25px;
  }

  .hero-photo-caption {
    right: 13px;
    bottom: 13px;
    left: 13px;
    align-items: flex-start;
    flex-direction: column;
    gap: 10px;
  }

  .hero-badge {
    top: -10px;
    right: -3px;
    width: 95px;
    height: 95px;
  }

  .floating-card {
    display: none;
  }

  .categories-section,
  .popular-section,
  .process-section {
    padding: 75px 0;
  }

  .builder-section {
    padding: 75px 0;
  }

  .builder-window-body {
    min-height: 315px;
    grid-template-columns: 104px 1fr;
  }

  .builder-menu {
    padding: 17px 9px;
  }

  .builder-menu button {
    padding: 10px;
  }


  .product-image {
    height: 335px;
  }

  .process-grid {
    grid-template-columns: 1fr;
  }

  .final-section {
    padding: 70px 0;
  }

  .footer-grid {
    grid-template-columns: 1fr;
    gap: 35px;
  }

  .footer-bottom {
    align-items: flex-start;
    flex-direction: column;
  }
}

/* Cake builder page */

.builder-page {
  min-height: 100vh;
  background: #fffaf7;
}

.builder-back-button {
  background: transparent;
  color: var(--brown);
  border: 1px solid var(--border);
}

.builder-back-button:hover {
  background: var(--brown);
  color: white;
}

.builder-hero {
  padding: 80px 0 64px;
  background:
    radial-gradient(circle at 85% 20%, #ffe2ec 0, transparent 28%),
    linear-gradient(135deg, #fffaf7, #fff2f5);
}

.builder-hero-inner {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 50px;
}

.builder-hero h1 {
  margin: 14px 0 0;
  color: var(--brown);
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(48px, 6vw, 76px);
  font-weight: 500;
  line-height: 1;
  letter-spacing: -3px;
}

.builder-hero h1 em {
  color: var(--pink);
  font-weight: 500;
}

.builder-hero p {
  max-width: 660px;
  margin: 25px 0 0;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.builder-step-indicator {
  min-width: 280px;
  display: flex;
  align-items: center;
  gap: 15px;
  padding: 20px;
  border: 1px solid var(--border);
  border-radius: 20px;
  background: rgba(255, 255, 255, 0.72);
  backdrop-filter: blur(12px);
}

.builder-step-indicator > span {
  width: 47px;
  height: 47px;
  display: grid;
  place-items: center;
  flex-shrink: 0;
  border-radius: 50%;
  background: var(--pink-light);
  color: var(--pink-dark);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 18px;
}

.builder-step-indicator div {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.builder-step-indicator strong {
  font-size: 14px;
}

.builder-step-indicator small {
  color: var(--muted);
  font-size: 11px;
}

.builder-workspace {
  padding: 70px 0 120px;
}

.builder-workspace-grid {
  display: grid;
  grid-template-columns: minmax(380px, 0.88fr) minmax(520px, 1.12fr);
  align-items: start;
  gap: 36px;
}

.preview-sticky {
  position: sticky;
  top: 110px;
  padding: 28px;
  border: 1px solid var(--border);
  border-radius: 30px;
  background: white;
  box-shadow: 0 28px 65px rgba(77, 49, 43, 0.09);
}

.preview-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
}

.preview-heading span {
  color: var(--pink-dark);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.preview-heading h2 {
  margin: 5px 0 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 30px;
  font-weight: 500;
}

.preview-weight {
  padding: 10px 14px;
  border-radius: 999px;
  background: var(--pink-light);
  color: var(--pink-dark) !important;
  font-size: 12px !important;
  letter-spacing: 0 !important;
  text-transform: none !important;
}

.cake-preview-stage {
  position: relative;
  min-height: 430px;
  display: grid;
  place-items: center;
  margin-top: 25px;
  overflow: hidden;
  border-radius: 24px;
  background:
    radial-gradient(circle at 50% 34%, white 0, #fde8ee 57%, #f5d8e1 100%);
}

.cake-preview {
  position: relative;
  z-index: 3;
  width: 270px;
  height: 220px;
  margin-top: 38px;
  filter: drop-shadow(0 24px 18px rgba(73, 47, 43, 0.18));
}

.cake-preview-body {
  position: absolute;
  right: 0;
  bottom: 0;
  left: 0;
  height: 165px;
  display: grid;
  place-items: center;
  overflow: hidden;
  border-radius: 25px 25px 34px 34px;
  box-shadow:
    inset 18px 0 24px rgba(255, 255, 255, 0.22),
    inset -20px 0 30px rgba(70, 38, 35, 0.09);
}

.cake-preview-top {
  position: absolute;
  z-index: 3;
  top: 32px;
  left: 50%;
  width: 100%;
  height: 72px;
  border-radius: 50%;
  transform: translateX(-50%);
}

.cake-shape-square .cake-preview-body {
  border-radius: 12px 12px 24px 24px;
}

.cake-shape-square .cake-preview-top {
  height: 58px;
  border-radius: 12px;
}

.cake-shape-heart {
  width: 265px;
}

.cake-shape-heart .cake-preview-body {
  border-radius: 45% 45% 28px 28px;
}

.cake-color-pink .cake-preview-body,
.cake-color-pink .cake-preview-top {
  background: linear-gradient(90deg, #f5a5bd, #ffd8e4, #e98ca8);
}

.cake-color-white .cake-preview-body,
.cake-color-white .cake-preview-top {
  background: linear-gradient(90deg, #efe7e2, #fffdfb, #e5d9d1);
}

.cake-color-blue .cake-preview-body,
.cake-color-blue .cake-preview-top {
  background: linear-gradient(90deg, #98d4e2, #d9f4f8, #75c0d2);
}

.cake-color-chocolate .cake-preview-body,
.cake-color-chocolate .cake-preview-top {
  background: linear-gradient(90deg, #71483f, #a46e61, #56352e);
}

.cake-inscription {
  max-width: 210px;
  padding: 9px 16px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.78);
  color: #a64565;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 15px;
  font-style: italic;
  text-align: center;
}

.preview-decoration {
  position: absolute;
  z-index: 6;
  top: -18px;
  left: 50%;
  display: flex;
  align-items: center;
  gap: 4px;
  transform: translateX(-50%);
}

.preview-berries span {
  font-size: 29px;
}

.preview-flowers {
  top: -23px;
  left: 67%;
}

.preview-flowers span {
  font-size: 31px;
}

.preview-pearls {
  position: absolute;
  z-index: 5;
  top: 20px;
  right: 18px;
  left: 18px;
  display: flex;
  justify-content: space-between;
}

.preview-pearls span {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: white;
  box-shadow: 0 2px 5px rgba(60, 37, 34, 0.15);
}

.preview-topper {
  position: absolute;
  z-index: 7;
  top: -68px;
  left: 50%;
  padding-bottom: 35px;
  transform: translateX(-50%);
}

.preview-topper::after {
  content: "";
  position: absolute;
  bottom: 0;
  left: 50%;
  width: 2px;
  height: 37px;
  background: #9d7146;
}

.preview-topper span {
  display: block;
  padding: 8px 12px;
  border-radius: 8px;
  background: #fff4dc;
  color: #7f5535;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 12px;
  white-space: nowrap;
}

.cake-preview-board {
  position: absolute;
  z-index: 2;
  bottom: 62px;
  width: 345px;
  height: 66px;
  border-radius: 50%;
  background: linear-gradient(#fff, #e9ddd7);
  box-shadow: 0 18px 28px rgba(72, 48, 43, 0.18);
}

.cake-preview-shadow {
  position: absolute;
  z-index: 1;
  bottom: 52px;
  width: 320px;
  height: 52px;
  border-radius: 50%;
  background: rgba(66, 42, 39, 0.14);
  filter: blur(18px);
}

.preview-summary {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  margin-top: 20px;
}

.preview-summary div {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 14px;
  border-radius: 15px;
  background: #fff7f5;
}

.preview-summary span {
  color: var(--muted);
  font-size: 10px;
}

.preview-summary strong {
  font-size: 12px;
}

.preview-note {
  margin: 17px 0 0;
  color: var(--muted);
  font-size: 11px;
  line-height: 1.6;
}

.builder-controls {
  display: grid;
  gap: 22px;
}

.builder-control-card,
.builder-order-card {
  padding: 30px;
  border: 1px solid var(--border);
  border-radius: 27px;
  background: white;
  box-shadow: 0 18px 45px rgba(76, 48, 42, 0.05);
}

.control-heading {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  margin-bottom: 25px;
}

.control-heading > span {
  width: 39px;
  height: 39px;
  display: grid;
  place-items: center;
  flex-shrink: 0;
  border-radius: 50%;
  background: var(--pink-light);
  color: var(--pink-dark);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 14px;
}

.control-heading h2 {
  margin: 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 24px;
  font-weight: 500;
}

.control-heading p {
  margin: 6px 0 0;
  color: var(--muted);
  font-size: 12px;
}

.shape-options,
.color-options {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.shape-options button,
.color-options button,
.weight-options button,
.filling-options button,
.decoration-options button {
  border: 1px solid var(--border);
  background: #fffaf8;
  cursor: pointer;
  transition:
    border 0.2s ease,
    background 0.2s ease,
    transform 0.2s ease;
}

.shape-options button:hover,
.color-options button:hover,
.weight-options button:hover,
.filling-options button:hover,
.decoration-options button:hover {
  transform: translateY(-2px);
}

.shape-options button.selected,
.color-options button.selected,
.weight-options button.selected,
.filling-options button.selected,
.decoration-options button.selected {
  border-color: var(--pink);
  background: var(--pink-light);
}

.shape-options button {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 13px;
  border-radius: 17px;
}

.shape-options strong,
.color-options strong {
  font-size: 12px;
}

.shape-icon {
  width: 46px;
  height: 46px;
  display: block;
  background: #f2b4c6;
  box-shadow: inset 0 0 0 4px rgba(255, 255, 255, 0.45);
}

.shape-icon-round {
  border-radius: 50%;
}

.shape-icon-square {
  border-radius: 8px;
}

.shape-heart-icon {
  color: #ef8fab;
  font-size: 54px;
  line-height: 1;
}

.weight-options {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 11px;
}

.weight-options button {
  min-height: 78px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 7px;
  border-radius: 16px;
}

.weight-options strong {
  font-size: 15px;
}

.weight-options span {
  color: var(--muted);
  font-size: 10px;
}

.filling-options {
  display: grid;
  gap: 11px;
}

.filling-options button {
  min-height: 77px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
  padding: 16px 18px;
  border-radius: 16px;
  text-align: left;
}

.filling-options button div {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.filling-options strong {
  font-size: 13px;
}

.filling-options span {
  color: var(--muted);
  font-size: 11px;
}

.filling-options small {
  color: var(--pink-dark);
  font-size: 11px;
  font-weight: 800;
  white-space: nowrap;
}

.color-options {
  grid-template-columns: repeat(4, 1fr);
}

.color-options button {
  min-height: 95px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 11px;
  border-radius: 16px;
}

.color-swatch {
  width: 39px;
  height: 39px;
  display: block;
  border: 4px solid white;
  border-radius: 50%;
  box-shadow: 0 0 0 1px var(--border);
}

.color-pink {
  background: #efa1b8;
}

.color-white {
  background: #f4ece7;
}

.color-blue {
  background: #91cedd;
}

.color-chocolate {
  background: #765046;
}

.decoration-options {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 11px;
}

.decoration-options button {
  min-height: 83px;
  display: flex;
  align-items: center;
  gap: 13px;
  padding: 14px;
  border-radius: 16px;
  text-align: left;
}

.decoration-icon {
  width: 43px;
  height: 43px;
  display: grid;
  flex-shrink: 0;
  place-items: center;
  border-radius: 13px;
  background: white;
  font-size: 21px;
}

.decoration-options button div {
  display: flex;
  flex: 1;
  flex-direction: column;
  gap: 4px;
}

.decoration-options strong {
  font-size: 13px;
}

.decoration-options small {
  color: var(--muted);
  font-size: 10px;
}

.option-check {
  width: 27px;
  height: 27px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: white;
  color: var(--pink-dark);
  font-size: 13px;
  font-weight: 900;
}

.builder-field {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-top: 17px;
}

.builder-field > span {
  font-size: 12px;
  font-weight: 700;
}

.builder-field input,
.builder-field textarea {
  width: 100%;
  border: 1px solid var(--border);
  border-radius: 15px;
  outline: none;
  background: #fffaf8;
  color: var(--brown);
  transition:
    border 0.2s ease,
    box-shadow 0.2s ease;
}

.builder-field input {
  min-height: 53px;
  padding: 0 55px 0 16px;
}

.builder-field textarea {
  padding: 15px 16px;
  resize: vertical;
}

.builder-field input:focus,
.builder-field textarea:focus {
  border-color: var(--pink);
  box-shadow: 0 0 0 4px rgba(236, 77, 132, 0.1);
}

.builder-field small {
  position: absolute;
  right: 15px;
  bottom: 17px;
  color: var(--muted);
  font-size: 9px;
}

.builder-order-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 30px;
  background: var(--brown);
  color: white;
}

.builder-order-card > div {
  display: flex;
  flex-direction: column;
}

.builder-order-card > div > span {
  color: rgba(255, 255, 255, 0.65);
  font-size: 11px;
}

.builder-order-card strong {
  margin-top: 5px;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 35px;
  font-weight: 500;
}

.builder-order-card p {
  max-width: 400px;
  margin: 9px 0 0;
  color: rgba(255, 255, 255, 0.56);
  font-size: 11px;
  line-height: 1.6;
}

.builder-submit-button {
  min-height: 56px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  gap: 18px;
  padding: 0 23px;
  border-radius: 999px;
  background: var(--pink);
  color: white;
  font-size: 13px;
  font-weight: 800;
  transition:
    transform 0.2s ease,
    background 0.2s ease;
}

.builder-submit-button:hover {
  background: var(--pink-dark);
  transform: translateY(-2px);
}

@media (max-width: 1050px) {
  .builder-hero-inner {
    align-items: flex-start;
    flex-direction: column;
  }

  .builder-workspace-grid {
    grid-template-columns: 1fr;
  }

  .preview-sticky {
    position: relative;
    top: auto;
  }
}

@media (max-width: 700px) {
  .builder-hero {
    padding: 55px 0 45px;
  }

  .builder-hero h1 {
    font-size: 45px;
    letter-spacing: -2px;
  }

  .builder-step-indicator {
    width: 100%;
    min-width: 0;
  }

  .builder-workspace {
    padding: 35px 0 75px;
  }

  .builder-workspace-grid {
    display: block;
  }

  .builder-live-preview {
    margin-bottom: 22px;
  }

  .preview-sticky,
  .builder-control-card,
  .builder-order-card {
    padding: 20px;
    border-radius: 22px;
  }

  .cake-preview-stage {
    min-height: 365px;
  }

  .cake-preview {
    transform: scale(0.82);
  }

  .cake-preview-board {
    width: 285px;
  }

  .preview-summary {
    grid-template-columns: 1fr;
  }

  .shape-options,
  .weight-options,
  .color-options,
  .decoration-options {
    grid-template-columns: repeat(2, 1fr);
  }

  .shape-options button:last-child {
    grid-column: 1 / -1;
  }

  .builder-order-card {
    align-items: stretch;
    flex-direction: column;
  }

  .builder-submit-button {
    width: 100%;
  }
}


/* Real cake preview */

.real-cake-stage {
  position: relative;
  min-height: 470px;
  display: grid;
  place-items: center;
  margin-top: 25px;
  overflow: hidden;
  border-radius: 25px;
  background:
    radial-gradient(circle at 50% 38%, #ffffff 0%, #fde9ef 56%, #f4d5df 100%);
}

.real-cake-layers {
  position: relative;
  z-index: 3;
  width: min(390px, 88%);
  aspect-ratio: 1 / 1;
}

.real-cake-layer {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  display: block;
  object-fit: contain;
  pointer-events: none;
}

.real-cake-base {
  z-index: 1;
  border-radius: 24px;
}

.real-cake-decoration {
  z-index: 3;
}

.real-cake-decoration-drips {
  z-index: 2;
}

.real-cake-decoration-pearls {
  z-index: 4;
}

.real-cake-decoration-topper {
  z-index: 6;
}

.real-cake-inscription {
  position: absolute;
  z-index: 7;
  right: 19%;
  bottom: 22%;
  left: 19%;
  padding: 8px 12px;
  overflow: hidden;
  color: #a24765;
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(12px, 1.6vw, 17px);
  font-style: italic;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
  text-shadow: 0 1px 1px rgba(255, 255, 255, 0.85);
  transform: rotate(-1deg);
}

.real-cake-shadow {
  position: absolute;
  z-index: 1;
  bottom: 48px;
  width: 330px;
  height: 55px;
  border-radius: 50%;
  background: rgba(68, 42, 38, 0.16);
  filter: blur(19px);
}

.preview-summary {
  grid-template-columns: repeat(2, 1fr);
}

.builder-color-options {
  grid-template-columns: repeat(3, 1fr);
}

.color-mint {
  background: #a9d8c8;
}

.color-lilac {
  background: #c5acd8;
}

.decoration-option-text {
  display: flex;
  flex: 1;
  align-items: flex-start;
  flex-direction: column;
  gap: 4px;
}

.decoration-option-text strong {
  font-size: 13px;
}

.decoration-option-text span {
  color: var(--muted);
  font-size: 10px;
  line-height: 1.45;
}

.decoration-option-text small {
  color: var(--pink-dark);
  font-size: 10px;
  font-weight: 800;
}

@media (max-width: 700px) {
  .real-cake-stage {
    min-height: 380px;
  }

  .real-cake-layers {
    width: min(330px, 92%);
  }

  .real-cake-shadow {
    width: 270px;
  }

  .builder-color-options {
    grid-template-columns: repeat(2, 1fr);
  }
}


/* Temporary clean cake base */

.temporary-cake-base {
  position: absolute;
  inset: 0;
  z-index: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.temporary-cake-top {
  position: absolute;
  top: 29%;
  width: 62%;
  height: 24%;
  border-radius: 50%;
  background: #f7d6df;
  box-shadow:
    inset 0 10px 18px rgba(255, 255, 255, 0.7),
    inset 0 -10px 18px rgba(110, 56, 72, 0.08);
}

.temporary-cake-side {
  position: absolute;
  top: 40%;
  width: 62%;
  height: 33%;
  border-radius: 0 0 26px 26px;
  background: #efc1ce;
  box-shadow:
    inset 14px 0 18px rgba(255, 255, 255, 0.18),
    inset -14px 0 18px rgba(95, 45, 60, 0.08),
    0 18px 24px rgba(89, 44, 57, 0.12);
}

.temporary-cake-board {
  position: absolute;
  top: 69%;
  width: 78%;
  height: 9%;
  border-radius: 50%;
  background: linear-gradient(
    180deg,
    #ffffff 0%,
    #e9e9e9 55%,
    #cfcfcf 100%
  );
  box-shadow: 0 14px 18px rgba(73, 45, 51, 0.16);
}

.temporary-cake-white .temporary-cake-top {
  background: #fffdf8;
}

.temporary-cake-white .temporary-cake-side {
  background: #f6f0e8;
}

.temporary-cake-pink .temporary-cake-top {
  background: #f9d9e1;
}

.temporary-cake-pink .temporary-cake-side {
  background: #efbdca;
}

.temporary-cake-blue .temporary-cake-top {
  background: #dbeaf5;
}

.temporary-cake-blue .temporary-cake-side {
  background: #bfd8e8;
}

.temporary-cake-mint .temporary-cake-top {
  background: #d6eee5;
}

.temporary-cake-mint .temporary-cake-side {
  background: #b9ddcf;
}

.temporary-cake-lilac .temporary-cake-top {
  background: #e6dcf1;
}

.temporary-cake-lilac .temporary-cake-side {
  background: #cebee1;
}

.temporary-cake-chocolate .temporary-cake-top {
  background: #8c5c4b;
}

.temporary-cake-chocolate .temporary-cake-side {
  background: #704536;
}
/* Cake builder preview */

.real-cake-stage {
  position: relative;
  min-height: 470px;
  display: grid;
  place-items: center;
  margin-top: 25px;
  overflow: hidden;
  border-radius: 25px;
  background:
    radial-gradient(
      circle at 50% 38%,
      #ffffff 0%,
      #fdf6f7 55%,
      #f6e5e9 100%
    );
}

.real-cake-layers {
  position: relative;
  z-index: 3;
  width: min(430px, 92%);
  aspect-ratio: 1 / 1;
}

.real-cake-layer {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  display: block;
  object-fit: contain;
  pointer-events: none;
}

.real-cake-base {
  z-index: 1;
}

.real-cake-decoration {
  z-index: 3;
}

.real-cake-decoration-drips {
  z-index: 2;
}

.real-cake-decoration-pearls {
  z-index: 4;
}

.real-cake-decoration-topper {
  z-index: 6;
}

.real-cake-inscription {
  position: absolute;
  z-index: 7;
  right: 19%;
  bottom: 22%;
  left: 19%;
  padding: 8px 12px;
  overflow: hidden;
  color: #a24765;
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(12px, 1.6vw, 17px);
  font-style: italic;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
  text-shadow: 0 1px 1px rgba(255, 255, 255, 0.85);
  transform: rotate(-1deg);
}

.real-cake-shadow {
  position: absolute;
  z-index: 1;
  bottom: 52px;
  width: 330px;
  height: 50px;
  border-radius: 50%;
  background: rgba(68, 42, 38, 0.13);
  filter: blur(18px);
}

.preview-summary {
  grid-template-columns: repeat(2, 1fr);
}

.builder-color-options {
  grid-template-columns: repeat(3, 1fr);
}

.color-white {
  background: #fffaf2;
}

.color-pink {
  background: #efb9c8;
}

.color-blue {
  background: #b8d9eb;
}

.color-mint {
  background: #b9ddcf;
}

.color-lilac {
  background: #cebee1;
}

.color-chocolate {
  background: #795141;
}

.decoration-option-text {
  display: flex;
  flex: 1;
  align-items: flex-start;
  flex-direction: column;
  gap: 4px;
}

.decoration-option-text strong {
  font-size: 13px;
}

.decoration-option-text span {
  color: var(--muted);
  font-size: 10px;
  line-height: 1.45;
}

.decoration-option-text small {
  color: var(--pink-dark);
  font-size: 10px;
  font-weight: 800;
}

@media (max-width: 700px) {
  .real-cake-stage {
    min-height: 380px;
  }

  .real-cake-layers {
    width: min(340px, 94%);
  }

  .real-cake-shadow {
    width: 270px;
  }

  .builder-color-options {
    grid-template-columns: repeat(2, 1fr);
  }
}

==================================================
FILE: lib/supabase.ts
==================================================
import { createClient } from "@supabase/supabase-js";

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(
  supabaseUrl,
  supabaseAnonKey,
  {
    auth: {
      persistSession: false,
      autoRefreshToken: false,
    },
  }
);

==================================================
FILE: package.json
==================================================
{
  "name": "milky-cake",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0",
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-rnd": "^10.5.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}


